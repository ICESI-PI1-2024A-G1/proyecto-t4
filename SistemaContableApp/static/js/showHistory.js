function showHistory(followingId) {
    // Redirige a la página que muestra el historial de cambios para la instancia de Following con el ID followingId
    window.location.href = "/historial/" + followingId + "/";
}
